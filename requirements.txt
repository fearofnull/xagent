# 飞书机器人依赖包

# 飞书开放平台SDK
lark-oapi>=1.2.0

# SSL证书处理
certifi>=2023.0.0

# 环境变量管理
python-dotenv>=1.0.0

# Pydantic 配置管理
pydantic>=2.0.0
pydantic-settings>=2.0.0

# 文件锁（用于会话管理并发控制）
filelock>=3.12.0

# 定时任务调度
schedule>=1.2.0
APScheduler>=3.10.4

# 测试框架（可选，用于单元测试）
pytest>=7.4.0
pytest-cov>=4.1.0

# 属性测试框架（可选，用于property-based testing）
hypothesis>=6.82.0

# AI API SDKs
anthropic>=0.39.0  # Claude API
google-generativeai>=0.8.0  # Gemini API
openai>=1.0.0  # OpenAI API

# Web Admin Interface
Flask>=3.0.0
Flask-CORS>=4.0.0
Flask-HTTPAuth>=4.8.0
Flask-Limiter>=3.5.0  # Rate limiting
PyJWT>=2.8.0
gunicorn>=21.2.0  # Production WSGI server
waitress>=3.0.0  # Production WSGI server (alternative to gunicorn, works on Windows)

# Performance optimizations (optional but recommended)
orjson>=3.9.0  # Fast JSON serialization (2-3x faster than standard json)

# AgentScope framework (for XAgent)
agentscope>=0.1.0

# Additional dependencies for XAgent
requests>=2.31.0  # HTTP requests
beautifulsoup4>=4.12.0  # HTML parsing
playwright>=1.40.0  # Browser automation (optional)
